# Copy to .env.production in your secure environment before production builds.
# This value is not a secret, but keep environment-specific values out of source as needed.
VITE_API_URL=https://<replace-backend-api-host>/api
